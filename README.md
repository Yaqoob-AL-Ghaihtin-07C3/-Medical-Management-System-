# نظام إدارة الصيدلية (Medical Management System)

## 📋 نظرة عامة على المشروع

نظام إدارة الصيدلية هو تطبيق ويب متكامل مصمم لإدارة العمليات اليومية للصيدليات بشكل احترافي وفعال. يوفر النظام واجهة سهلة الاستخدام لإدارة المخزون الدوائي، معالجة الفواتير، إدارة المستخدمين، وإنشاء التقارير التفصيلية.

### 🎯 الهدف من المشروع

تطوير نظام إلكتروني شامل يساعد الصيدليات على:
- تنظيم وإدارة المخزون الدوائي بكفاءة عالية
- تسريع عمليات البيع وإصدار الفواتير
- تتبع المبيعات والتقارير المالية
- إدارة المستخدمين والصلاحيات
- توفير واجهة عربية سهلة الاستخدام

---

## 🛠️ التقنيات المستخدمة

### 1. **PHP (لغة البرمجة الخلفية)**
- **الإصدار**: PHP 8.0.9
- **السبب**: 
  - لغة مفتوحة المصدر ومجانية
  - سهولة التكامل مع MySQL
  - دعم واسع للاستضافة المشتركة
  - مناسبة لتطوير تطبيقات الويب الديناميكية
  - توفر مكتبات جاهزة للتعامل مع قواعد البيانات والجلسات

### 2. **MySQL (قاعدة البيانات)**
- **الإصدار**: MariaDB 10.4.20
- **السبب**:
  - نظام إدارة قواعد بيانات علائقي قوي
  - أداء عالي في التعامل مع البيانات
  - دعم ممتاز للغة العربية (UTF-8)
  - سهولة النسخ الاحتياطي والاستعادة
  - تكامل مثالي مع PHP

### 3. **HTML5 & CSS3 (واجهة المستخدم)**
- **السبب**:
  - HTML5: لبناء هيكل صفحات الويب بشكل دلالي
  - CSS3: لتنسيق وتجميل الواجهات
  - دعم الاستجابة للأجهزة المختلفة

### 4. **JavaScript & jQuery (البرمجة الأمامية)**
- **السبب**:
  - إضافة التفاعلية للصفحات
  - التحقق من البيانات قبل الإرسال
  - تحسين تجربة المستخدم (AJAX)
  - معالجة الأحداث بشكل ديناميكي

### 5. **Bootstrap (إطار عمل CSS)**
- **السبب**:
  - تصميم سريع واحترافي
  - واجهات متجاوبة (Responsive)
  - مكونات جاهزة (Modals, Tables, Forms)

### 6. **Font Awesome (الأيقونات)**
- **السبب**:
  - مكتبة أيقونات غنية
  - سهولة الاستخدام
  - تحسين الواجهة البصرية

---

## 📁 هيكل المشروع

```
Medical_Management_System/
│
├── assets/              # الملفات المساعدة الأساسية
│   ├── css/            # ملفات التنسيق
│   ├── js/             # ملفات JavaScript
│   ├── font-awesome/   # مكتبة الأيقونات
│   ├── fonts/          # الخطوط المستخدمة
│   ├── photo/          # الصور والملفات المرفوعة
│   ├── db.php          # ملف الاتصال بقاعدة البيانات
│   ├── function.php    # الدوال المساعدة
│   ├── autoloader.php  # تحميل المكتبات تلقائياً
│   └── bill.php        # معالجة الفواتير
│
├── db/                  # قاعدة البيانات
│   └── store.sql       # ملف SQL لإنشاء القاعدة
│
├── include/             # ملفات مضمنة إضافية
│   ├── db.php
│   ├── function.php
│   ├── autoloader.php
│   └── bill.php
│
├── index.php            # الصفحة الرئيسية
├── login.php            # صفحة تسجيل الدخول
├── logout.php           # تسجيل الخروج
├── addnew.php           # إضافة دواء جديد
├── addUser.php          # إضافة مستخدم جديد
├── manageUsers.php      # إدارة المستخدمين
├── manageCat.php        # إدارة التصنيفات
├── inventeries.php      # عرض المخزون
├── billing.php          # صفحة الفواتير
├── billout.php          # إتمام الفاتورة
├── reports.php          # التقارير
├── profile.php          # الملف الشخصي
├── accountSetting.php   # إعدادات الحساب
├── delete.php           # حذف العناصر
├── clear.php            # مسح البيانات
└── called.php           # معالجة طلبات AJAX
```

---

## 📂 شرح المجلدات الرئيسية

### 1. **مجلد `assets/`**
يحتوي على جميع الموارد والملفات المساعدة للنظام:

- **`css/`**: ملفات التنسيق (Bootstrap, Custom Styles)
- **`js/`**: مكتبات JavaScript (jQuery, DataTables, Bootstrap JS)
- **`font-awesome/`**: أيقونات Font Awesome
- **`fonts/`**: الخطوط المستخدمة في النظام
- **`photo/`**: مجلد تخزين الصور (صور الأدوية، المستخدمين، التصنيفات)
- **`db.php`**: ملف الاتصال بقاعدة البيانات وتهيئة المتغيرات العامة
- **`function.php`**: دوال مساعدة (مثل: `siteTitle()`, `siteName()`, `getAdminName()`)
- **`autoloader.php`**: تحميل المكتبات الخارجية تلقائياً
- **`bill.php`**: معالجة عمليات الفواتير والسلة

### 2. **مجلد `db/`**
يحتوي على ملف قاعدة البيانات:

- **`store.sql`**: ملف SQL يحتوي على بنية الجداول والبيانات الأولية
  - يتم استيراده عبر phpMyAdmin
  - يحتوي على 6 جداول رئيسية

### 3. **مجلد `include/`**
نسخة احتياطية أو بديلة من الملفات المساعدة الموجودة في `assets/`

---

## 📄 شرح الملفات الرئيسية

### 1. **`login.php` - صفحة تسجيل الدخول**

**الوظيفة**: 
- عرض نموذج تسجيل الدخول
- التحقق من بيانات المستخدم (البريد الإلكتروني وكلمة المرور)
- إنشاء جلسة (Session) للمستخدم عند النجاح

**آلية العمل**:
```
1. المستخدم يدخل البريد الإلكتروني وكلمة المرور
2. يتم إرسال البيانات عبر POST
3. الاستعلام في قاعدة البيانات للتحقق من الصحة
4. إذا كانت البيانات صحيحة:
   - إنشاء Session وتخزين userId
   - تهيئة مصفوفة الفواتير $_SESSION['bill']
   - إعادة التوجيه إلى index.php
5. إذا كانت خاطئة: عرض رسالة خطأ
```

**الكود الأساسي**:
```php
$result = $con->query("SELECT * FROM users WHERE email='$user' AND password='$pass'");
if($result->num_rows > 0) {
    session_start();
    $data = $result->fetch_assoc();
    $_SESSION['userId'] = $data['id'];
    $_SESSION['bill'] = array();
    header('location:index.php');
}
```

---

### 2. **`logout.php` - تسجيل الخروج**

**الوظيفة**:
- إنهاء جلسة المستخدم
- مسح جميع بيانات الجلسة
- إعادة التوجيه إلى صفحة تسجيل الدخول

**آلية العمل**:
```php
session_start();
session_destroy();
header('location:login.php');
```

---

### 3. **`index.php` - الصفحة الرئيسية (لوحة التحكم)**

**الوظيفة**:
- عرض جميع تصنيفات الأدوية
- عرض عدد الأدوية المتوفرة في كل تصنيف
- إمكانية إضافة تصنيف جديد
- عرض معلومات المستخدم الحالي

**المكونات الرئيسية**:
1. **التحقق من الجلسة**: التأكد من تسجيل دخول المستخدم
2. **القائمة الجانبية**: روابط التنقل الرئيسية
3. **عرض التصنيفات**: بطاقات تفاعلية لكل تصنيف
4. **نافذة منبثقة**: لإضافة تصنيف جديد

**الاستعلامات المستخدمة**:
```php
// جلب جميع التصنيفات
$array = $con->query("SELECT id, name, pic FROM categories");

// عد الأدوية لكل تصنيف
$stmt_count = $con->prepare("SELECT COUNT(*) AS total_medicines FROM inventeries WHERE catId = ?");
```

---

### 4. **`inventeries.php` - عرض المخزون**

**الوظيفة**:
- عرض جميع الأدوية أو الأدوية حسب التصنيف
- إمكانية إضافة الدواء إلى الفاتورة
- حذف الأدوية من المخزون
- استخدام DataTables للبحث والترتيب

**المميزات**:
- جدول تفاعلي مع إمكانية البحث
- عرض تفاصيل الدواء (الاسم، القوة، السعر، المورد، الشركة)
- زر "بيع" لإضافة الدواء للفاتورة
- زر "حذف" لإزالة الدواء

**الكود الأساسي**:
```php
if (isset($_GET['catId'])) {
    $catId = $_GET['catId'];
    $stockArray = $con->query("SELECT * FROM inventeries WHERE catId='$catId'");
} else {
    $stockArray = $con->query("SELECT * FROM inventeries");
}
```

---

### 5. **`addnew.php` - إضافة دواء جديد**

**الوظيفة**:
- نموذج لإضافة دواء جديد للمخزون
- إدخال معلومات الدواء (الاسم، التصنيف، المورد، السعر، القوة، الشركة، الوصف)
- رفع صورة للدواء

**البيانات المطلوبة**:
- اسم الدواء
- التصنيف (Category)
- المورد (Supplier)
- القوة/الجرعة (Unit)
- السعر (Price)
- الشركة المصنعة (Company)
- الوصف (Description)
- الصورة (اختياري)

**معالجة البيانات**:
```php
if (isset($_POST['save'])) {
    // رفع الصورة
    move_uploaded_file($_FILES["pic"]["tmp_name"], "photo/".$filename);
    
    // إدخال البيانات
    $con->query("INSERT INTO inventeries (catId, supplier, name, unit, price, pic, description, company) 
                 VALUES (...)");
}
```

---

### 6. **`billing.php` - صفحة الفاتورة**

**الوظيفة**:
- عرض الأدوية المضافة للفاتورة الحالية
- تعديل الكمية لكل دواء
- حساب الإجمالي
- إمكانية إتمام الفاتورة

**آلية العمل**:
1. عرض جميع العناصر في `$_SESSION['bill']`
2. لكل عنصر: عرض الاسم، السعر، الكمية
3. حساب الإجمالي = مجموع (السعر × الكمية)
4. نافذة منبثقة لإدخال معلومات المشتري

**الكود الأساسي**:
```php
foreach ($_SESSION['bill'] as $row) {
    echo "<tr>";
    echo "<td>$row[name]</td>";
    echo "<td>USD. $row[price]</td>";
    echo "<td>الكمية</td>";
    echo "</tr>";
    $total = $total + $row['price'] * $row['qty'];
}
```

---

### 7. **`billout.php` - إتمام الفاتورة**

**الوظيفة**:
- حفظ الفاتورة في قاعدة البيانات
- تسجيل معلومات المشتري (الاسم، رقم الهاتف، الخصم)
- حساب المبلغ النهائي بعد الخصم
- مسح سلة الفواتير

**البيانات المحفوظة**:
- اسم المشتري
- رقم الهاتف
- نسبة الخصم
- عدد الأصناف
- المبلغ الإجمالي
- معرف المستخدم (الصيدلي)
- تاريخ الفاتورة

**معالجة البيانات**:
```php
if (isset($_POST['billInfo'])) {
    $name = $_POST['name'];
    $contact = $_POST['contact'];
    $discount = $_POST['discount'];
    $amount = $total - ($total * $discount / 100);
    
    $con->query("INSERT INTO sold (name, contact, discount, item, amount, userId) 
                 VALUES (...)");
    
    $_SESSION['bill'] = array(); // مسح السلة
}
```

---

### 8. **`reports.php` - التقارير**

**الوظيفة**:
- عرض جميع الفواتير المحفوظة
- تفاصيل كل فاتورة (المشتري، المبلغ، الخصم، التاريخ)
- معرفة الموظف الذي أنشأ الفاتورة
- استخدام DataTables للبحث والفلترة

**المعلومات المعروضة**:
- التسلسل
- اسم المشتري
- رقم الهاتف
- الخصم
- المبلغ المستحق
- اسم الموظف
- تاريخ الإنشاء

**الاستعلام**:
```php
$array = $con->query("SELECT * FROM sold ORDER BY date DESC");
```

---

### 9. **`manageCat.php` - إدارة التصنيفات**

**الوظيفة**:
- عرض جميع التصنيفات
- إضافة تصنيف جديد
- تعديل تصنيف موجود
- حذف تصنيف

**العمليات**:
- **إضافة**: اسم التصنيف + صورة
- **تعديل**: تحديث الاسم أو الصورة
- **حذف**: إزالة التصنيف (مع التحقق من عدم وجود أدوية مرتبطة)

---

### 10. **`addUser.php` - إضافة مستخدم جديد**

**الوظيفة**:
- إضافة موظف/صيدلي جديد للنظام
- تحديد صلاحيات الوصول

**البيانات المطلوبة**:
- البريد الإلكتروني
- كلمة المرور
- الاسم
- رقم الهاتف
- الصورة الشخصية

---

### 11. **`profile.php` - الملف الشخصي**

**الوظيفة**:
- عرض معلومات المستخدم الحالي
- تعديل البيانات الشخصية
- تغيير الصورة الشخصية

---

### 12. **`accountSetting.php` - إعدادات الحساب**

**الوظيفة**:
- تغيير كلمة المرور
- تحديث البريد الإلكتروني
- تعديل رقم الهاتف

---

### 13. **`delete.php` - حذف العناصر**

**الوظيفة**:
- حذف الأدوية من المخزون
- حذف التصنيفات
- حذف المستخدمين

**آلية العمل**:
```php
if (isset($_GET['item'])) {
    $id = $_GET['item'];
    $con->query("DELETE FROM inventeries WHERE id='$id'");
    header('location:inventeries.php');
}
```

---

### 14. **`clear.php` - مسح البيانات**

**الوظيفة**:
- مسح سلة الفواتير الحالية
- إعادة تعيين الجلسة

---

### 15. **`called.php` - معالجة طلبات AJAX**

**الوظيفة**:
- إضافة دواء إلى الفاتورة عبر AJAX
- إزالة دواء من الفاتورة
- معالجة الطلبات الديناميكية دون إعادة تحميل الصفحة

**مثال**:
```php
if (isset($_GET['q']) && $_GET['q'] == 'addtobill') {
    $id = $_POST['id'];
    // جلب بيانات الدواء
    $result = $con->query("SELECT * FROM inventeries WHERE id='$id'");
    $row = $result->fetch_assoc();
    
    // إضافة للسلة
    $_SESSION['bill'][] = array(
        'id' => $row['id'],
        'name' => $row['name'],
        'price' => $row['price'],
        'qty' => 1
    );
}
```

---

## 🗄️ قاعدة البيانات (Database)

### اسم القاعدة: `store`

### الجداول الرئيسية:

#### 1. **جدول `users` - المستخدمين**
يحتوي على معلومات الموظفين والصيادلة:

| العمود | النوع | الوصف |
|--------|-------|-------|
| `id` | INT (Primary Key) | المعرف الفريد |
| `email` | VARCHAR(222) | البريد الإلكتروني |
| `password` | VARCHAR(222) | كلمة المرور |
| `name` | VARCHAR(222) | الاسم |
| `pic` | VARCHAR(222) | الصورة الشخصية |
| `number` | VARCHAR(222) | رقم الهاتف |
| `date` | TIMESTAMP | تاريخ التسجيل |

**الدور**: 
- تخزين بيانات المستخدمين
- المصادقة عند تسجيل الدخول
- تتبع من قام بإنشاء الفواتير

---

#### 2. **جدول `categories` - التصنيفات**
يحتوي على تصنيفات الأدوية:

| العمود | النوع | الوصف |
|--------|-------|-------|
| `id` | INT (Primary Key) | المعرف الفريد |
| `name` | VARCHAR(222) | اسم التصنيف |
| `pic` | VARCHAR(111) | صورة التصنيف |
| `details` | VARCHAR(333) | التفاصيل |
| `date` | TIMESTAMP | تاريخ الإنشاء |

**أمثلة على التصنيفات**:
- شراب
- أبر (حقن)
- كبسول
- مسكنات الألم
- حبوب
- كريم
- مرهم حروق

**الدور**:
- تنظيم الأدوية حسب النوع
- تسهيل البحث والفلترة

---

#### 3. **جدول `inventeries` - المخزون**
يحتوي على جميع الأدوية المتوفرة:

| العمود | النوع | الوصف |
|--------|-------|-------|
| `id` | INT (Primary Key) | المعرف الفريد |
| `catId` | INT (Foreign Key) | معرف التصنيف |
| `supplier` | VARCHAR(222) | اسم المورد |
| `name` | TEXT | اسم الدواء |
| `unit` | TEXT | القوة/الجرعة (مثل: 500mg) |
| `price` | TEXT | السعر |
| `pic` | TEXT | صورة الدواء |
| `description` | TEXT | الوصف |
| `company` | VARCHAR(111) | الشركة المصنعة |
| `date` | TIMESTAMP | تاريخ الإضافة |

**الدور**:
- تخزين معلومات الأدوية
- ربط الأدوية بالتصنيفات
- توفير بيانات للفواتير

---

#### 4. **جدول `sold` - الفواتير**
يحتوي على سجل المبيعات:

| العمود | النوع | الوصف |
|--------|-------|-------|
| `id` | INT (Primary Key) | المعرف الفريد |
| `name` | VARCHAR(222) | اسم المشتري |
| `contact` | VARCHAR(222) | رقم الهاتف |
| `discount` | VARCHAR(222) | نسبة الخصم |
| `item` | VARCHAR(222) | عدد الأصناف |
| `amount` | VARCHAR(222) | المبلغ الإجمالي |
| `userId` | INT (Foreign Key) | معرف الموظف |
| `date` | TIMESTAMP | تاريخ الفاتورة |

**الدور**:
- تسجيل جميع المبيعات
- إنشاء التقارير المالية
- تتبع أداء الموظفين

---

#### 5. **جدول `supplier` - الموردين**
يحتوي على معلومات الموردين:

| العمود | النوع | الوصف |
|--------|-------|-------|
| `id` | INT (Primary Key) | المعرف الفريد |
| `name` | TEXT | اسم المورد |
| `pic` | TEXT | الصورة |
| `number` | TEXT | رقم الهاتف |
| `address` | TEXT | العنوان |
| `cnic` | TEXT | رقم الهوية |
| `date` | TIMESTAMP | تاريخ التسجيل |

**الدور**:
- تخزين بيانات الموردين
- الربط مع الأدوية

---

#### 6. **جدول `site` - إعدادات النظام**
يحتوي على معلومات النظام:

| العمود | النوع | الوصف |
|--------|-------|-------|
| `id` | INT (Primary Key) | المعرف الفريد |
| `title` | VARCHAR(222) | عنوان النظام |
| `name` | VARCHAR(222) | اسم الصيدلية |

**الدور**:
- تخزين إعدادات النظام العامة
- عرض اسم الصيدلية في الواجهة

---

### العلاقات بين الجداول:

```
users (1) -----> (N) sold
  └─ userId في جدول sold يشير إلى id في جدول users

categories (1) -----> (N) inventeries
  └─ catId في جدول inventeries يشير إلى id في جدول categories

supplier (1) -----> (N) inventeries
  └─ supplier في جدول inventeries يشير إلى name في جدول supplier
```

---

## 🔐 آلية تسجيل الدخول والخروج

### تسجيل الدخول (Login):

1. **المستخدم يدخل البيانات**:
   - البريد الإلكتروني
   - كلمة المرور

2. **التحقق من البيانات**:
   ```php
   $result = $con->query("SELECT * FROM users WHERE email='$user' AND password='$pass'");
   ```

3. **إذا كانت البيانات صحيحة**:
   - إنشاء جلسة جديدة: `session_start()`
   - تخزين معرف المستخدم: `$_SESSION['userId'] = $data['id']`
   - تهيئة سلة الفواتير: `$_SESSION['bill'] = array()`
   - إعادة التوجيه: `header('location:index.php')`

4. **إذا كانت خاطئة**:
   - عرض رسالة خطأ: "Login Error! Try again."

### تسجيل الخروج (Logout):

1. **بدء الجلسة**: `session_start()`
2. **تدمير الجلسة**: `session_destroy()`
3. **إعادة التوجيه**: `header('location:login.php')`

### حماية الصفحات:

جميع الصفحات محمية بالكود التالي:
```php
session_start();
if(!isset($_SESSION['userId'])) {
    header('location:login.php');
    exit();
}
```

---

## 👥 إدارة المستخدمين

### الوظائف:

1. **إضافة مستخدم جديد** (`addUser.php`):
   - إدخال البيانات (الاسم، البريد، كلمة المرور، الصورة)
   - حفظ في جدول `users`

2. **عرض المستخدمين** (`manageUsers.php`):
   - جدول بجميع المستخدمين
   - إمكانية التعديل والحذف

3. **الملف الشخصي** (`profile.php`):
   - عرض بيانات المستخدم الحالي
   - تعديل المعلومات

4. **إعدادات الحساب** (`accountSetting.php`):
   - تغيير كلمة المرور
   - تحديث البريد الإلكتروني

---

## 💰 إدارة الفواتير

### خطوات إنشاء فاتورة:

1. **اختيار الأدوية** (`inventeries.php`):
   - تصفح المخزون
   - الضغط على زر "بيع" لإضافة الدواء للفاتورة
   - يتم إرسال طلب AJAX إلى `called.php`
   - إضافة الدواء إلى `$_SESSION['bill']`

2. **مراجعة الفاتورة** (`billing.php`):
   - عرض جميع الأدوية المختارة
   - تعديل الكمية لكل دواء
   - حساب الإجمالي تلقائياً
   - إمكانية حذف دواء من الفاتورة

3. **إتمام الفاتورة** (`billout.php`):
   - إدخال معلومات المشتري (الاسم، الهاتف)
   - تحديد نسبة الخصم
   - حساب المبلغ النهائي
   - حفظ الفاتورة في جدول `sold`
   - مسح السلة

### معادلة حساب المبلغ:
```
المبلغ النهائي = الإجمالي - (الإجمالي × نسبة الخصم / 100)
```

---

## 📦 إدارة المخزون

### الوظائف:

1. **عرض المخزون** (`inventeries.php`):
   - جدول تفاعلي بجميع الأدوية
   - البحث والفلترة باستخدام DataTables
   - عرض حسب التصنيف

2. **إضافة دواء جديد** (`addnew.php`):
   - نموذج شامل لإدخال البيانات
   - رفع صورة الدواء
   - اختيار التصنيف والمورد

3. **حذف دواء** (`delete.php`):
   - حذف من جدول `inventeries`
   - إعادة التوجيه للمخزون

4. **إدارة التصنيفات** (`manageCat.php`):
   - إضافة/تعديل/حذف التصنيفات
   - ربط الأدوية بالتصنيفات

---

## 📊 التقارير

### أنواع التقارير:

1. **تقرير المبيعات** (`reports.php`):
   - جميع الفواتير مرتبة حسب التاريخ
   - تفاصيل كل فاتورة
   - اسم الموظف الذي أنشأ الفاتورة
   - إمكانية البحث والفلترة

2. **تقرير المخزون**:
   - عدد الأدوية لكل تصنيف
   - الأدوية المتوفرة

---

## 🎨 واجهة المستخدم

### المكونات:

1. **القائمة الجانبية**:
   - لوحة التحكم
   - الأدوية
   - إضافة دواء جديد
   - الفواتير
   - الإعدادات

2. **الشريط العلوي**:
   - اسم المستخدم
   - الصورة الشخصية
   - قائمة منسدلة (الملف الشخصي، تسجيل الخروج)

3. **المحتوى الرئيسي**:
   - عرض البيانات في جداول
   - بطاقات تفاعلية
   - نماذج إدخال

---

## 🔧 ملفات المساعدة

### 1. **`assets/db.php`**
```php
$con = new mysqli('localhost', 'root', '123456', 'store');
$array = $con->query("SELECT * FROM users WHERE id ='$_SESSION[userId]'");
$user = $array->fetch_assoc();
$base_path = "http://localhost/dev.test/store";
```
**الوظيفة**:
- إنشاء اتصال بقاعدة البيانات
- جلب بيانات المستخدم الحالي
- تعريف المسار الأساسي

### 2. **`assets/function.php`**
دوال مساعدة مثل:
- `siteTitle()`: إرجاع عنوان الموقع
- `siteName()`: إرجاع اسم الصيدلية
- `getAdminName($userId)`: إرجاع اسم الموظف

### 3. **`assets/autoloader.php`**
تحميل المكتبات الخارجية:
- Bootstrap CSS/JS
- jQuery
- Font Awesome
- DataTables

### 4. **`assets/bill.php`**
معالجة عمليات الفواتير والسلة

---

## 📝 نقاط للمناقشة الشفوية

### 1. **نظرة عامة**:
- "المشروع عبارة عن نظام إدارة صيدلية متكامل يهدف إلى أتمتة العمليات اليومية"
- "تم بناؤه باستخدام PHP وMySQL مع واجهة Bootstrap العربية"

### 2. **اختيار التقنيات**:
- "اخترنا PHP لسهولة التكامل مع MySQL ودعمها الواسع"
- "MySQL لقوتها في إدارة البيانات العلائقية"
- "Bootstrap لتصميم واجهة احترافية ومتجاوبة"

### 3. **قاعدة البيانات**:
- "القاعدة تحتوي على 6 جداول رئيسية مترابطة"
- "جدول users للمستخدمين، categories للتصنيفات، inventeries للمخزون، sold للفواتير"
- "العلاقات: One-to-Many بين categories و inventeries، وبين users و sold"

### 4. **الأمان**:
- "جميع الصفحات محمية بنظام الجلسات (Sessions)"
- "التحقق من تسجيل الدخول في كل صفحة"
- "استخدام htmlspecialchars() لمنع XSS"
- "يُنصح باستخدام Prepared Statements لمنع SQL Injection"

### 5. **آلية الفواتير**:
- "يتم تخزين الفاتورة مؤقتاً في Session حتى الإتمام"
- "استخدام AJAX لإضافة الأدوية دون إعادة تحميل الصفحة"
- "حساب الإجمالي ديناميكياً مع دعم الخصومات"

### 6. **المميزات**:
- "واجهة عربية كاملة سهلة الاستخدام"
- "نظام تصنيفات مرن"
- "تقارير مفصلة للمبيعات"
- "إدارة متعددة المستخدمين"

### 7. **التحديات**:
- "التعامل مع الترميز العربي (UTF-8)"
- "إدارة الجلسات بشكل آمن"
- "تصميم قاعدة بيانات فعالة"

### 8. **التحسينات المستقبلية**:
- "إضافة نظام صلاحيات متقدم (Admin, User, Viewer)"
- "تطبيق Prepared Statements لجميع الاستعلامات"
- "إضافة نظام إشعارات للأدوية منتهية الصلاحية"
- "تصدير التقارير إلى PDF أو Excel"
- "إضافة لوحة تحكم إحصائية (Dashboard)"
- "تطبيق REST API للتكامل مع أنظمة أخرى"

### 9. **الاستخدام العملي**:
- "النظام جاهز للاستخدام في الصيدليات الصغيرة والمتوسطة"
- "يمكن نشره على استضافة مشتركة بسهولة"
- "يدعم عدة مستخدمين في نفس الوقت"

### 10. **الخلاصة**:
- "المشروع يوفر حلاً شاملاً لإدارة الصيدليات"
- "سهل الصيانة والتطوير"
- "قابل للتوسع والتخصيص حسب الحاجة"

---

## 🚀 طريقة التثبيت والتشغيل

### المتطلبات:
- XAMPP (أو WAMP/MAMP)
- PHP 8.0 أو أحدث
- MySQL/MariaDB
- متصفح ويب حديث

### خطوات التثبيت:

1. **تثبيت XAMPP**:
   - تحميل وتثبيت XAMPP

2. **نسخ المشروع**:
   ```
   نسخ مجلد medical إلى: C:\xampp\htdocs\
   ```

3. **إنشاء قاعدة البيانات**:
   - فتح phpMyAdmin: `http://localhost/phpmyadmin`
   - إنشاء قاعدة بيانات جديدة باسم `store`
   - استيراد ملف `db/store.sql`

4. **تعديل إعدادات الاتصال** (إذا لزم الأمر):
   - فتح `assets/db.php`
   - تعديل بيانات الاتصال:
     ```php
     $con = new mysqli('localhost', 'root', 'كلمة_المرور', 'store');
     ```

5. **تشغيل المشروع**:
   - تشغيل Apache و MySQL من XAMPP
   - فتح المتصفح: `http://localhost/medical/login.php`

6. **تسجيل الدخول**:
   - البريد: `admin1@gmail.com`
   - كلمة المرور: `admin`

---

## 📞 معلومات إضافية

### البيانات الافتراضية:
- **المستخدمون**: يوجد مستخدمان في القاعدة
- **التصنيفات**: 12 تصنيف جاهز
- **الأدوية**: 3 أدوية كأمثلة

### الصلاحيات:
- حالياً جميع المستخدمين لديهم نفس الصلاحيات
- يمكن تطوير نظام صلاحيات متقدم

---

## 📚 المراجع والمصادر

- **PHP Documentation**: https://www.php.net/docs.php
- **MySQL Documentation**: https://dev.mysql.com/doc/
- **Bootstrap**: https://getbootstrap.com/
- **jQuery**: https://jquery.com/
- **DataTables**: https://datatables.net/
- **Font Awesome**: https://fontawesome.com/

---

## ✅ الخاتمة

نظام إدارة الصيدلية هو مشروع متكامل يجمع بين البساطة والفعالية. تم تصميمه ليكون سهل الاستخدام للصيادلة مع توفير جميع الوظائف الأساسية لإدارة الصيدلية بكفاءة. النظام قابل للتطوير والتوسع ليشمل مميزات إضافية حسب احتياجات المستخدم.

---

**تم إعداد هذا التوثيق بواسطة**: نظام إدارة الصيدلية  
**التاريخ**: ديسمبر 2021  
**الإصدار**: 1.0

---

## 📄 رخصة الاستخدام

هذا المشروع مفتوح المصدر ويمكن استخدامه وتعديله بحرية للأغراض التعليمية والتجارية.
